import { Callout } from "nextra/components";

# Day 12 — Sorting Algorithms II

<Callout type="info">
    This chapter is being written. Check back soon!
</Callout>

## What you'll learn here

- **QuickSort** with both Lomuto and Hoare partition schemes
- **HeapSort** — sorting using a binary heap in O(n log n) with O(1) extra space
- **Counting Sort, Radix Sort, Bucket Sort** — when O(n) sorting is actually possible
- How to pick the right sort for the right input

In the meantime, [Day 11 — Sorting Algorithms I](/day11) introduces the comparison-based sorts (bubble, selection, insertion, merge) that this chapter builds on.
